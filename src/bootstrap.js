import React from 'react';
import ReactDOM from 'react-dom';
import AppContainer from './AppContainer';

const container = document.querySelector('#root');
ReactDOM.render(<AppContainer language="en" />, container);
